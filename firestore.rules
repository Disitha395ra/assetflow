rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Replace this rule with your company identity provider before production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
